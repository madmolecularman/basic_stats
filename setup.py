from setuptools import setup

setup(
    name = 'norm_testing',
    version = '0.0.4',
    author = 'Jacob Michael Green',
    packages=['norm_testing'],
    install_requires=['numpy','matplotlib'],
)
